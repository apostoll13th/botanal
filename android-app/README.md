# 💰 Семейный Бюджет - Android App

Современное Android приложение для управления семейным бюджетом с красивым Material Design 3.

## 🎨 Дизайн

- **Material Design 3** - самый новый дизайн от Google
- **Jetpack Compose** - декларативный UI фреймворк
- **Градиентные карточки** с анимациями
- **Glassmorphism** эффекты
- **Темная/Светлая тема** из коробки
- **Плавные анимации** и transitions

## ✨ Возможности

### 📊 Главный экран
- Красивый градиентный хедер
- Карточки с доходами, расходами и балансом
- Список последних операций
- Анимированные компоненты

### 💸 Операции
- Добавление доходов/расходов
- Фильтрация по категориям
- FAB кнопка для быстрого добавления
- Разноцветные иконки для типов операций

### 💼 Бюджеты
- Планирование расходов по категориям
- Прогресс-бары с цветовой индикацией
- Автоматический расчет использования

### 📝 Мемосы
- Личный дневник
- Заметки с заголовками
- Красивые карточки
- Быстрое добавление

### 🎁 Список желаний
- Семейные желания
- Приоритеты (Высокий/Средний/Низкий)
- Поддержка изображений
- Отметка исполненных

## 🛠 Технологии

- **Kotlin** - современный язык для Android
- **Jetpack Compose** - UI toolkit
- **Material 3** - дизайн система
- **Navigation Compose** - навигация
- **Retrofit** - HTTP клиент для API
- **Coil** - загрузка изображений
- **DataStore** - хранение настроек

## 🚀 Установка

### Требования
- Android Studio Hedgehog (2023.1.1) или новее
- JDK 17+
- Android SDK 34
- Минимальная версия Android: 7.0 (API 24)

### Шаги установки

1. **Откройте проект в Android Studio:**
   ```bash
   cd android-app
   ```

2. **Синхронизируйте Gradle:**
   - File → Sync Project with Gradle Files

3. **Настройте API endpoint:**
   - Откройте `app/src/main/java/com/familybudget/network/ApiConfig.kt`
   - Укажите URL вашего Go backend в зависимости от вашего случая:

   **Для локальной разработки:**
   ```kotlin
   // Если запускаете на Android эмуляторе
   const val BASE_URL = "http://10.0.2.2:5000/api/"

   // Если запускаете на физическом устройстве (замените на IP вашего компьютера)
   const val BASE_URL = "http://192.168.1.100:5000/api/"
   ```

   **Для продакшена:**
   ```kotlin
   const val BASE_URL = "http://YOUR_SERVER_IP:5000/api/"
   // или
   const val BASE_URL = "https://yourdomain.com/api/"
   ```

   **Как узнать IP вашего компьютера:**
   - Windows: `ipconfig` (искать IPv4 Address)
   - macOS/Linux: `ifconfig` или `ip addr` (искать inet адрес)

   **Важно:** Убедитесь что ваш Go backend запущен и доступен по сети!

4. **Запустите приложение:**
   - Выберите устройство (эмулятор или физическое)
   - Нажмите Run (▶️)

## 📱 Скриншоты

### Главный экран
- Градиентный хедер с анимацией
- Карточки с суммами
- Список операций

### Операции
- Добавление расходов/доходов
- Категории с иконками
- FAB для быстрого доступа

### Бюджеты
- Прогресс по категориям
- Цветовая индикация
- Лимиты и расходы

### Мемосы
- Дневник
- Заметки
- Красивые карточки

### Список желаний
- Семейные мечты
- Приоритеты
- Изображения

## 🎨 Цветовая палитра

```kotlin
// Primary gradient
val GradientPurpleStart = Color(0xFF667eea)
val GradientPurpleMiddle = Color(0xFF764ba2)
val GradientPurpleEnd = Color(0xFFf093fb)

// Memos
val MemosBlue = Color(0xFF667eea)
val MemosPurple = Color(0xFF764ba2)

// Wishlist
val WishlistPink = Color(0xFFf093fb)
val WishlistRed = Color(0xFFf5576c)

// Status colors
val SuccessGreen = Color(0xFF10b981)
val ErrorRed = Color(0xFFef4444)
val WarningOrange = Color(0xFFf59e0b)
```

## 📦 Структура проекта

```
android-app/
├── app/
│   ├── src/
│   │   └── main/
│   │       ├── java/com/familybudget/
│   │       │   ├── ui/
│   │       │   │   ├── components/   # Переиспользуемые компоненты
│   │       │   │   ├── screens/      # Экраны приложения
│   │       │   │   └── theme/        # Темы и стили
│   │       │   ├── viewmodels/       # ViewModels для управления состоянием
│   │       │   ├── network/          # API интеграция (Retrofit)
│   │       │   ├── models/           # Модели данных
│   │       │   └── FamilyBudgetApp.kt
│   │       ├── res/
│   │       │   ├── values/           # Ресурсы (strings, colors)
│   │       │   └── drawable/         # Иконки и изображения
│   │       └── AndroidManifest.xml
│   └── build.gradle.kts
├── build.gradle.kts
├── settings.gradle.kts
└── README.md
```

## 🔗 Интеграция с API

Приложение полностью интегрировано с вашим существующим Go backend через REST API.

### Архитектура:
- **Retrofit** - HTTP клиент для работы с API
- **ViewModels** - управление состоянием и бизнес-логикой
- **StateFlow** - реактивное обновление UI
- **Coroutines** - асинхронные операции

### Реализованные endpoints:

**Операции (Expenses):**
- `GET /api/expenses` - получить все операции
- `POST /api/expenses` - добавить операцию
- `PUT /api/expenses/{id}` - обновить операцию
- `DELETE /api/expenses/{id}` - удалить операцию
- `GET /api/expenses/summary` - получить сводку

**Бюджеты (Budgets):**
- `GET /api/budgets` - получить бюджеты
- `POST /api/budgets` - создать бюджет
- `PUT /api/budgets/{id}` - обновить бюджет
- `DELETE /api/budgets/{id}` - удалить бюджет

**Мемосы (Memos):**
- `GET /api/memos` - получить мемосы
- `POST /api/memos` - создать мемос
- `PUT /api/memos/{id}` - обновить мемос
- `DELETE /api/memos/{id}` - удалить мемос

**Список желаний (Wishlist):**
- `GET /api/wishlist` - получить список желаний
- `POST /api/wishlist` - добавить желание
- `PUT /api/wishlist/{id}` - обновить желание
- `DELETE /api/wishlist/{id}` - удалить желание

**Категории и пользователи:**
- `GET /api/categories` - получить категории
- `GET /api/users` - получить пользователей
- `GET /api/savings-goals` - получить цели экономии

## 🎯 Следующие шаги

1. ✅ **API интеграция** - подключен реальный backend
2. **Авторизация** - добавить login/logout экран
3. **Offline mode** - Room database для кеширования данных
4. **Push уведомления** - напоминания о бюджете и целях
5. **Виджеты** - быстрый доступ к балансу с главного экрана
6. **Экспорт данных** - PDF/Excel отчеты
7. **Графики и аналитика** - визуализация расходов
8. **Фильтры и поиск** - расширенный поиск по операциям

## 📄 Лицензия

Частное приложение для семейного использования.

## 👨‍💻 Разработка

Разработано с использованием лучших практик Android:
- Clean Architecture
- MVVM паттерн
- Material Design Guidelines
- Accessibility support

---

**Сделано с ❤️ для вашей семьи**
