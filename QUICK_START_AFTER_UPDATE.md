# 🚀 БЫСТРЫЙ СТАРТ ПОСЛЕ ОБНОВЛЕНИЯ

Все зависимости обновлены до последних версий (Ноябрь 2025).
Следуйте этим шагам для запуска проекта.

---

## ⚡ БЫСТРЫЕ КОМАНДЫ

### 1. Go Backend (обязательно первым!)

```bash
cd backend-go

# Проверить версию Go (должна быть >= 1.23)
go version

# Если Go < 1.23, скачайте с https://go.dev/dl/

# Обновить зависимости
go mod tidy
go mod download

# Проверить компиляцию
go build

# Запустить сервер
go run main.go

# Сервер должен запуститься на http://localhost:5000
```

**Ожидаемый вывод:**
```
[GIN-debug] Listening and serving HTTP on :5000
Database connected successfully
Migrations completed
```

---

### 2. React Frontend

```bash
cd frontend-react

# Удалить старые зависимости
rm -rf node_modules package-lock.json

# Установить обновлённые зависимости
npm install

# Запустить dev сервер
npm start

# Откроется http://localhost:3000
```

**Возможные warning'и (нормально):**
```
npm WARN deprecated ...
# Это нормально для переходного периода
```

**Проверьте:**
- ✅ Главная страница загружается
- ✅ Графики отображаются
- ✅ API запросы работают
- ✅ Тёмная тема переключается
- ✅ PWA установка доступна

---

### 3. Android App

```bash
cd android-app

# Открыть проект в Android Studio
# File → Open → выбрать папку android-app/
```

**В Android Studio:**

1. **Sync Gradle** (автоматически запустится)
   - Если не запустилось: File → Sync Project with Gradle Files

2. **Ждать загрузки** (первый раз 5-10 минут)
   - Gradle скачает все зависимости
   - Compose BOM 2025.10.01 (~50MB)
   - Kotlin 2.2.21 compiler

3. **Проверить ошибки**
   - Build → Make Project
   - Должно пройти без ошибок ✅

4. **Настроить API endpoint**
   ```kotlin
   // android-app/app/src/main/java/com/familybudget/network/ApiConfig.kt

   // Для эмулятора:
   const val BASE_URL = "http://10.0.2.2:5000/api/"

   // Для физического устройства (замените на IP вашего компьютера):
   const val BASE_URL = "http://192.168.1.100:5000/api/"
   ```

5. **Запустить приложение**
   - Run → Run 'app'
   - Выбрать эмулятор или устройство

**Проверьте:**
- ✅ Приложение запускается
- ✅ Dashboard загружает данные
- ✅ Изображения загружаются (Coil 3 network)
- ✅ Навигация работает
- ✅ API запросы выполняются

---

## 🐛 РЕШЕНИЕ ПРОБЛЕМ

### Go Backend

**Проблема:** `go: module requires Go >= 1.23`
```bash
# Решение: Обновить Go
# https://go.dev/dl/
# Скачать и установить Go 1.23+
```

**Проблема:** `cannot find package`
```bash
# Решение:
go clean -modcache
go mod tidy
go mod download
```

---

### React Frontend

**Проблема:** `npm ERR! peer dependency conflict`
```bash
# Решение:
rm -rf node_modules package-lock.json
npm cache clean --force
npm install --legacy-peer-deps
```

**Проблема:** `Module not found: Can't resolve 'axios'`
```bash
# Решение:
npm install
# Перезапустить dev сервер
```

**Проблема:** `Failed to load resource: net::ERR_CONNECTION_REFUSED`
```bash
# Решение: Go backend не запущен
cd backend-go && go run main.go
```

---

### Android App

**Проблема:** `Could not resolve com.squareup.retrofit2:retrofit:3.0.0`
```bash
# Решение 1: Проверить интернет соединение
# Решение 2: Очистить Gradle cache
./gradlew clean
./gradlew --refresh-dependencies
```

**Проблема:** `Unresolved reference: coil3`
```bash
# Решение: Sync Gradle ещё раз
# File → Sync Project with Gradle Files
# Ждать завершения
```

**Проблема:** `Task :app:compileDebugKotlin FAILED`
```bash
# Решение: Проверить версию Kotlin
# android-app/build.gradle.kts
# Должно быть: kotlin("android") version "2.2.21"

# Затем:
./gradlew clean build
```

**Проблема:** `Cannot connect to 10.0.2.2:5000`
```bash
# Для эмулятора - это нормальный адрес
# Убедитесь что Go backend запущен:
cd backend-go && go run main.go

# Для физического устройства - замените на реальный IP:
# Узнать IP:
# Windows: ipconfig
# Linux/Mac: ifconfig или ip addr
```

---

## ✅ ПРОВЕРОЧНЫЙ ЧЕКЛИСТ

После запуска всех компонентов:

### Backend (http://localhost:5000)
- [ ] `curl http://localhost:5000/api/health` возвращает OK
- [ ] Логи показывают "Database connected"
- [ ] Нет ошибок в консоли

### Frontend (http://localhost:3000)
- [ ] Страница загружается
- [ ] Можно залогиниться (admin/adminStrongPass!)
- [ ] Графики отображаются
- [ ] API запросы работают (Network tab в DevTools)
- [ ] Тёмная тема переключается
- [ ] PWA кнопка установки видна

### Android App
- [ ] Приложение собирается без ошибок
- [ ] Dashboard показывает данные
- [ ] Можно добавить операцию (хотя бы try)
- [ ] Навигация между экранами работает
- [ ] Изображения загружаются (если есть)
- [ ] Нет crash'ей при открытии экранов

---

## 📊 ТЕСТОВЫЕ ДАННЫЕ

Используйте тестовый аккаунт для входа в Web UI:

```
Логин: admin
Пароль: adminStrongPass!
```

Или:
```
Логин: analyst
Пароль: analystGo#2024
```

---

## 🆘 СРОЧНАЯ ПОМОЩЬ

Если ничего не работает:

1. **Откат на предыдущий коммит:**
   ```bash
   git log --oneline -5
   # Найти коммит до обновлений
   git reset --hard <commit-hash>
   ```

2. **Проверка версий:**
   ```bash
   # Go
   go version  # должно быть >= go1.23

   # Node
   node --version  # желательно 18+
   npm --version

   # Android Studio
   # Help → About → должно быть Iguana (2023.2) или новее
   ```

3. **Логи с ошибками:**
   - Go: stdout/stderr
   - React: Browser Console (F12)
   - Android: Logcat в Android Studio

4. **Создать issue на GitHub** с:
   - Версией компонента
   - Полным текстом ошибки
   - Шагами для воспроизведения

---

## 🎓 ПОЛЕЗНЫЕ ССЫЛКИ

### Документация обновлений:
- `UPDATE_NOTES.md` - детальный changelog
- `VERIFICATION_REPORT.md` - полный отчёт проверки
- `README.md` - общая информация о проекте

### Официальные доки:
- **React 19**: https://react.dev/blog/2025/10/01/react-19-2
- **Kotlin 2.2**: https://kotlinlang.org/docs/releases.html
- **Compose 2025.10**: https://developer.android.com/jetpack/androidx/releases/compose
- **Retrofit 3.0**: https://github.com/square/retrofit/releases/tag/v3.0.0
- **Gin 1.11**: https://github.com/gin-gonic/gin/releases/tag/v1.11.0

---

## 🚀 PRODUCTION DEPLOY

Когда всё протестировано локально:

```bash
# 1. Собрать production билды
cd frontend-react && npm run build
cd android-app && ./gradlew assembleRelease
cd backend-go && go build -o server

# 2. Docker Compose (если используется)
docker-compose build
docker-compose up -d

# 3. Проверить health
curl http://your-server/api/health
```

---

**Успешного запуска! 🎉**

Если всё заработало - проект готов к production использованию с последними стабильными версиями всех зависимостей.
