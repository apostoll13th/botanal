- 🔑 Единственный владелец схемы и миграций — Go backend (`backend-go/schema.go`). Любые изменения БД описываем здесь.
- 🤖 Telegram-бот больше не трогает DDL: только `wait_for_db()` и бизнес-логика (добавление расходов и т.п.).
- 🧩 PWA (React) теперь создает операции/категории/профили через REST (`POST /api/expenses`, `/api/categories`, `/api/users`). Продолжаем развивать UI как основной инструмент аналитики.
- 📚 При добавлении новых сущностей: сначала миграция в Go → эндпоинты → UI → документация (`README`, `DATABASE_ARCHITECTURE.md`, `ANALYTICS_SERVICE_GUIDE.md`).
